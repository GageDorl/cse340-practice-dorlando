<%- include('../partials/header') %>
<main>
    <h1>Faculty Directory</h1>
    <p>Browse our faculty members and click on any member to see detailed information including their title and department.</p>

    <div class="faculty-list">
        <% faculty.forEach(member => { %>
            <div class="faculty-card"><a href="/faculty/<%= member.id %>">
                <h3><%= member.name %></h3>
                <p class="faculty-department"><%= member.department %></p>
                <p class="faculty-title"><%= member.title %></p>
            </a></div>
        <% }); %>
    </div>
</main>
<%- include('../partials/footer') %>